# Judge Node Template Contract V1

Templates are deployment-owned records containing an ID, display name, fixed executable/arguments, bounded resource budget and capability profile. Only enabled template IDs may be selected by an administrator. Product/Web can submit template ID and count only.
