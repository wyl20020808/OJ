# PHASE 1E-R Web Recovery Context

Branch: `codex/phase1er-judge-ui-recovery`  
Worktree: `D:\OJPlatform-worktrees\phase1b-web-authoring`

Preserve: synthetic qualification-only labeling, no verdict deception, existing status mapping and unknown fallback. Do not implement a Judge, Redis access, or backend/Auth internals.

Required: W01-W10; Lead owns W11-W12. Allowed: `apps/web/**`, Web/browser tests, worker report `Docs/reports/OJPLATFORM_1E_R_WORKSTREAM_WEB_REPORT.md`. Forbidden: API internals, Redis/queue, Auth modules, central composition, root manifests, PROJECT_STATUS, shared contracts and fake real verdicts.

Use public API only. Record shared needs as Integration Requests. Stop after committed scoped evidence or immediately if a synthetic state could be represented as a real execution verdict.

