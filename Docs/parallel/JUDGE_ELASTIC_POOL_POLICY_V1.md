# Judge Elastic Pool Policy V1

The Judge Service owns MANUAL/AUTOMATIC mode, trusted template, min/max nodes, queue wait targets, pending threshold, normal/fast scale steps, idle window, independent cooldowns, utilization threshold, host CPU/RAM reserves and control version. MANUAL never changes desired count; AUTOMATIC is bounded by policy and host capacity.
