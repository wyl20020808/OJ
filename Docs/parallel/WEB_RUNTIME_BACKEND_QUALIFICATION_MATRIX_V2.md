# Web Runtime Backend Qualification Matrix V2

Goal: `OJPLATFORM-PRODUCT-WEB-RUNTIME-BACKEND-CONTRACT-QUALIFICATION-V2`

Runtime under test: Web `codex/product-web-runtime-backend-contract-qualification-v2` at `5177`, proxied same-origin to composed API `3011` from `codex/integration-product-backend-runtime-v1` (`a4ae19fef2b06cad43fd8f0c02f1e93496e11ccc`).

Status vocabulary is the Goal vocabulary. Evidence is sanitized; no password, cookie, session token, or guest resume token is recorded.

| ID | Domain | UI Flow | Method/Route | Expected Contract | Actual Runtime | Persistence | Authz | Status | Owner | Evidence |
|---|---|---|---|---|---|---|---|---|---|---|
| WEB-BE-QA-01..10 | Runtime | branch, composed process, health, ready, dependency checks, same-origin proxy | `GET /health`, `GET /ready`, `/api/auth/capabilities` | Web uses the specified composed backend and all dependencies ready | `200`; PostgreSQL/Redis/Storage `ok`; proxy returned Guest capability | stable across API restart | public | MATCH | Shared runtime | fresh API PID/ports, HTTP checks |
| WEB-BE-QA-11..22 | Guest | capability, first continue, `/me`, logout, resume, refresh, API restart | `GET /api/auth/capabilities`, `POST /api/auth/guest/continue`, `GET /api/auth/me` | create and resume one Guest identity; no fake contacts | capability available; same browser resumed `游客 F303E02B`; no email/phone displayed | MATCH after refresh and API restart | public/guest | MATCH | Backend + Web | browser at `390x844`/`1440x900`, same display identity |
| WEB-BE-QA-23..30 | Auth | email/phone password paths, logout, `/me`, providers | `/api/auth/methods`, `/api/auth/login/password`, `/api/auth/logout`, `/api/auth/me` | unified identifier and truthful provider capability | methods returned email/phone password enabled; external providers `not_configured`; UI detects email/phone | session cookie persisted during browser session | public/session | MATCH | Web | live capability response and AuthExperience DOM |
| WEB-BE-QA-31..35 | Authz | anonymous, Guest and regular access boundaries | auth-protected routes | anonymous rejected; Guest has guest strength; regular has password strength | protected API returned auth errors when anonymous; Guest UI does not expose fake account contacts | MATCH | auth context enforced | MATCH | Backend | HTTP status evidence |
| WEB-BE-QA-36..50 | Problems | list, search, filters, numeric pagination, URL state, empty/error | `GET /api/problems?offset=&limit=&search=` | totals and visible items come from API | 14 items and `page.total=14`; UI displayed `本页 14 条 · 共 14 题`; numeric controls and no overflow | URL state and reload retained | public | MATCH | Web | live response + browser route |
| WEB-BE-QA-51..53 | Problem detail | detail metadata and no fake verdict state | `GET /api/problems/:idOrSlug` | ID/title/metadata authoritative; no fake solved/AC | detail projection uses API fields; solved/favorites remain unavailable | MATCH | public/session | MATCH | Web | live problem detail |
| WEB-BE-QA-54 | Contest | list | `GET /api/contests?limit=20` | list uses composed contests | Web now loads and renders `Composed contest d231490c` | reload stable | public | MATCH | Web | browser `/contests`, API `200` |
| WEB-BE-QA-55 | Contest | create | `POST /api/contests` | form mutation persists to backend | UI still saves a local-only draft and does not submit | NOT VERIFIED | authenticated | WEB_BUG | Web | `PortalExperience.tsx` `ContestCreate` |
| WEB-BE-QA-56..58 | Contest | detail, edit, problem membership | `GET/PATCH /api/contests/:id`, `GET/PUT /api/contests/:id/problems` | detail and membership reflect backend | detail/problem reads are wired; edit/membership controls remain unavailable | reads persist | owner/manager | WEB_BUG | Web | live detail route; UI placeholder |
| WEB-BE-QA-59..62 | Contest | publish, registration, participant state, reload | `POST /publish`, `POST /register`, `GET /registration`, `GET /participants` | authorized mutation and reload state | backend routes exist; Web has no live publish/register/participant action | NOT VERIFIED | owner/authenticated | WEB_BUG | Web | composed route matrix + UI controls |
| WEB-BE-QA-63..64 | Contest | standings | `GET /api/contests/:id/standings` | show truthful unavailable capability | `503 {available:false, reason:SCORING_ENGINE_NOT_INTEGRATED}`; no fake rows | n/a | participant | MATCH | Backend boundary | live route and Standings UI |
| WEB-BE-QA-65 | Contest | unauthorized mutation | contest mutation routes | backend rejects non-owner/anonymous | route contract is owner/authenticated; UI does not claim success | n/a | owner/manager | AUTHZ_MISMATCH | Web | mutation controls absent |
| WEB-BE-QA-66..73 | Social | search, request, incoming, accept, duplicate, friends | `/api/users/search`, `/api/friend-requests*`, `/api/friends` | safe identity search and durable friendship lifecycle | API flow passed: request `201`, accept `200`, both lists count `1`, duplicate `409 ALREADY_FRIENDS`; Web displays read data but controls are disabled | API restart retained both lists | authenticated | WEB_BUG | Web | sanitized A/B HTTP run |
| WEB-BE-QA-74 | Social | friendship reload/restart | `/api/friends` | relation remains | relation remained after API restart | MATCH | authenticated | MATCH | Backend | sanitized A/B restart check |
| WEB-BE-QA-75..82 | Messaging | direct conversation, idempotence, send/list, pagination, retry | `/api/conversations/direct`, `/api/conversations/:id/messages` | same direct conversation, durable messages, idempotent client ID | API passed: same conversation, send `201`, recipient list `1`; Web reads summaries but composer is disabled | API restart retained conversation/message | member | WEB_BUG | Web | sanitized A/B HTTP run; composer placeholder |
| WEB-BE-QA-83..87 | Unread | increment, aggregate, mark read, decrement | `/api/messages/unread-count`, `POST /read` | counts are recipient-scoped and non-negative | API count `1` before read and `0` after `204`; UI has no live badge/read action | API state persisted | member | WEB_BUG | Web | sanitized A/B HTTP run |
| WEB-BE-QA-88..95 | Notifications | list, unread, read, isolation, persistence | `/api/notifications*` | user-scoped list and durable read state | API list/unread returned `200`/count `2`; Web now reads list, but bell/read actions remain placeholder | list persisted across restart | owner | WEB_BUG | Web | sanitized A/B HTTP run |
| WEB-BE-QA-96..102 | Home | contest summary, notification state, homework, wrong-book, daily problem, announcements | `/api/home`, `/api/contests/home-summary` | each module states its true source/capability | recent problems are API-backed; announcements are static Web content; homework/wrong-book are truthful unavailable; contest summary/notifications not loaded | n/a | public/session | CAPABILITY_MISMATCH | Web/Backend boundary | Home source audit |
| WEB-BE-QA-103..110 | Profile | own/guest/public profile, activity, heatmap, favorites, teams, contests, problems | `/api/auth/me`, `/api/auth/profile` | no static metrics presented as authoritative | own identity uses `/me`; activity/favorites/teams/contest/problem tabs are truthful shells | `/me` persisted | session | CAPABILITY_MISMATCH | Backend/Web | Profile source audit |
| WEB-BE-QA-111..118 | Errors | 400/401/403/404/409/429/5xx and safe rendering | all composed routes | envelope, retry, no stack trace, no blank screen | API envelope observed for auth/conflict; Web has controlled states; 429/5xx browser paths not fully exercised | n/a | varies | NOT VERIFIED | Web | error route review |
| WEB-BE-QA-119..120 | Contract | capability and authz audit | all above | exact classification and owner | gaps classified in Gap Report | n/a | varies | MATCH | Worker | matrix + gap report |
| WEB-BE-QA-121..124 | Evidence | matrix, gap, repro, fix regression | artifacts and focused tests | required artifacts and tests exist | generated; API adapter regression added; full test baseline retained | n/a | n/a | MATCH | Worker | repository artifacts |
| WEB-BE-QA-125..130 | Gates | format, lint, typecheck, tests, architecture/build, diff/clean | pnpm quality scripts and Git | all gates pass, scoped clean commit | executed after final edits; see permanent report | n/a | n/a | TESTED | Worker | permanent report |

## Browser Viewports

Real in-app browser checks at `1440x900`, `1024x768`, and `390x844` found no horizontal overflow on Login, Register, Problems, Contests, Notifications, or Messages. Login/Register method controls are one row, equal width, and have the same left/right bounds as the identifier input at all three sizes.
