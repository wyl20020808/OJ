# PHASE 1E Integration Plan

1. Bootstrap the frozen Judge Protocol/Queue contract and rotate the three permanent slots from one approved baseline.
2. Workers implement only their exclusive ownership scopes and publish evidence reports.
3. Lead audits ownership and integration requests, composes APIs/routes/migrations, and qualifies real PostgreSQL/Redis retry and crash behavior.
4. Lead qualifies the deterministic fake boundary and browser status journey, then runs full regression and closes the phase.

No user-source execution, real Judge, Sandbox, Contest, or Phase 2 work is in scope.

