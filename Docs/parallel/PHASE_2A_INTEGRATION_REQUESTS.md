# Phase 2A Integration Requests

Workers must append requests using:

`INTEGRATION REQUEST: requested change; reason; affected file; expected contract impact; tests required.`

No requests are open at Bootstrap. Lead alone resolves changes to shared contracts, central API composition, runtime orchestration, migration registry, root manifests, lockfiles, CI, and `PROJECT_STATUS`.
