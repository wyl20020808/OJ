# PHASE 1E Fake Judge Qualification

The qualification fixture uses deterministic control input and synthetic metadata only. Assertions cover enqueue, lease, duplicate delivery, retry, stale lease recovery, terminal exhaustion, API/Redis restart persistence, and explicit `FAKE` labeling. The fixture never reads, compiles, imports, evaluates, shells, or executes user source, and synthetic outcomes are never treated as real verdicts or scores.

